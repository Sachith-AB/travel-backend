# Test Configuration - Reduces database connections for testing
server.port=0

# Use pooled connection with minimal connections
spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.dnouvojlwvsahnhzfhyf
spring.datasource.password=Hirun5001000
spring.datasource.driver-class-name=org.postgresql.Driver

# Minimal HikariCP configuration for tests
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.max-lifetime=300000
spring.datasource.hikari.leak-detection-threshold=30000

# PostgreSQL specific settings
spring.datasource.hikari.data-source-properties.prepareThreshold=0
spring.datasource.hikari.data-source-properties.preparedStatementCacheQueries=0
spring.datasource.hikari.data-source-properties.preparedStatementCacheSizeMiB=0

# JPA Configuration for tests
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Disable initialization
spring.sql.init.mode=never
spring.liquibase.enabled=false

# Logging
logging.level.org.hibernate=WARN
logging.level.org.springframework=WARN
